# Build and Install by Haskell Stack

**This is not Official Guide!**

```
# clone haskell packages as submodules
$ git submodule update --init --recursive

$ stack build
$ stack install semantics
```
